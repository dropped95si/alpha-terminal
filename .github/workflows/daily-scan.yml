name: Daily Signal Scan

on:
  schedule:
    - cron: '0 9 * * 1-5'  # 9am UTC, Monday-Friday
  workflow_dispatch:  # Manual trigger

jobs:
  scan-and-upload:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install supabase
      
      - name: Run scan & upload to Supabase
        env:
          SUPABASE_URL: ${{ https://gyilkjvdglnzbzioyqze.supabase.co }}
          SUPABASE_KEY: ${{ eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imd5aWxranZkZ2xuemJ6aW95cXplIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg3ODM2NTksImV4cCI6MjA4NDM1OTY1OX0.Zk2GQma5RBJPNR1u_3xiwVeXzptLpZPFOyh7uCIOMXg }}
        run: |
          python scripts/upload_signals.py
      
      - name: Send notification (optional)
        if: always()
        run: |
          echo "✅ Daily scan completed"
```

---

## **Step 3: Add Supabase Secrets to GitHub**

1. Go: **https://github.com/dropped95si/alpha-terminal**
2. Settings → **Secrets and variables** → **Actions**
3. Click **"New repository secret"**

Add these:

**Secret 1: SUPABASE_URL**
```
Name: SUPABASE_URL
Value: (paste from Supabase → Settings → API → Project URL)
```

**Secret 2: SUPABASE_KEY**
```
Name: SUPABASE_KEY
Value: (paste from Supabase → Settings → API → anon public)
